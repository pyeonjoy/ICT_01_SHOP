<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="order">
<!-- cart_list -->
<!-- order_pay -->
	<select id="paydetail" parameterType="int" resultType="pvo">
		select *
		from shop_product
		where product_idx= 4
	</select>
</mapper>